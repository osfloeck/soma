[project]
name = "soma"
version = "0.1.0"
description = "Basic static site generator"
authors = [{name="Oskar", email="oskar.floeck@icloud.com"}]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.2.1",
]

[project.scripts]
soma = "soma.cli:cli"
